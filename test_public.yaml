# Public Pipeline Test - SRR24010030
# Run from vibrion-public/: snakemake -s workflow/Snakefile --configfile test_public.yaml --cores 4

# Input/Output Paths (Relative to vibrion-public root)
samples_dir: "../data/raw_reads"
output_dir: "results_test"
reference_dir: "../data/references"

# Databases (Point to main project DBs to avoid re-downloading)
kraken_db: "../data/kraken2_standard_8gb"
mmseqs_db: "../data/mmseqs_db/swissprot"
mmseqs_min_seq_id: 0.8

# Hostile Configuration
hostile_index: "human-t2t-hla-argos985"

# Taxonomic Filtering
vibrio_taxid: 662

# API Configuration
nvidia_api_key: "nvapi-XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX"
evo2_url: "https://health.api.nvidia.com/v1/biology/arc/evo2-40b/generate"

# Analysis Parameters
threads: 4
memory_mb: 8000
aligner_type: "bwa"
mapping_rescue_enabled: True

# Samples
samples:
  - SRR24010030